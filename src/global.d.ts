declare const alertbox: {
    render: (options: {
      alertIcon: string;
      title: string;
      message: string;
      btnTitle: string;
      themeColor: string;
      btnColor: string;
      border: boolean;
    }) => void;
  };
  